/** query param used to express whether the user should receive a notification to verify their account */
export const NOTIFY_UNVERIFIED_QUERY_PARAM = "notify-verification";

/** Can't use booleans as query param values. Use this to express `true` */
export const PARAM_VALUE_TRUE = "true";
