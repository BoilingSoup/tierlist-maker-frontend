import { FlexProps, GroupProps } from "@mantine/core";

export const formFlexProps: FlexProps = {
  direction: "column",
  maw: 500,
  mx: "auto",
  mt: "5rem",
  gap: "lg"
};

export const formSubmitProps: GroupProps = {
  position: "right",
  mt: "md"
};
